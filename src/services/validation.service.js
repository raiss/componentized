export function validateInput(inputId, inputVal) {
  // TODO: need to be done, the below funtion is for the test to pass.
  return {
    isValid: false,
    validationReport: [
      {
        title: "unknown",
        details: "unknown element!"
      }
    ]
  }
};
